[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
markers =
    asyncio: mark a test as an asyncio test
    slow: mark test as slow (deselect with '-m "not slow"')
    integration: mark as integration test (deselect with '-m "not integration"')
